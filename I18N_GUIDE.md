# 国际化 (i18n) 功能说明

## 🌍 功能概述

项目已支持中英文双语切换，用户可以在设置界面或顶部导航栏中自由切换语言。

## ✨ 功能特性

### 1. 双语言支持
- **中文 (zh-CN)** - 默认语言
- **English (en-US)** - 英文

### 2. 语言切换方式

#### 方式一：顶部导航栏快速切换
- 在页面右上角的主题切换按钮旁边
- 点击下拉选择器即可切换语言
- 带有国旗图标便于识别

#### 方式二：设置界面切换
- 点击右上角的设置图标 ⚙️
- 在"外观设置"部分找到"语言"选项
- 从下拉菜单中选择目标语言

### 3. 持久化存储
- 语言偏好自动保存到 `localStorage`
- 刷新页面后保持选择的语言
- 下次访问时自动应用上次选择的语言

## 📁 文件结构

```
src/
├── locales/
│   ├── index.js          # i18n 核心模块
│   ├── zh-CN.js          # 中文语言包
│   └── en-US.js          # 英文语言包
├── components/
│   ├── SettingsModal.vue # 设置界面（含语言切换）
│   └── ...
└── App.vue               # 主应用（含导航栏语言切换）
```

## 🔧 技术实现

### 1. i18n 核心模块 (`src/locales/index.js`)

提供以下功能：
- `t(key, params)` - 翻译函数，支持参数替换
- `setLocale(locale)` - 设置语言并保存到 localStorage
- `getLocale()` - 获取当前语言
- `getSupportedLocales()` - 获取支持的语言列表
- `getLocaleName(locale)` - 获取语言显示名称

### 2. 语言包结构

每个语言包包含以下模块：
- `app` - 应用基本信息
- `common` - 通用文本（确认、取消等）
- `nav` - 导航栏文本
- `stats` - 统计信息
- `card` - 卡片组件文本
- `addModal` - 添加/编辑弹窗文本
- `settings` - 设置界面文本
- `chart` - 图表组件文本
- `confirm` - 确认对话框文本
- `providers` - 平台名称
- `errors` - 错误提示

### 3. 使用示例

```javascript
import { t } from './locales/index.js'

// 基本用法
const title = t('app.title') // "API 余额查询工具" 或 "API Balance Checker"

// 带参数的翻译
const message = t('settings.importConfirm', { count: 5 })
// "确定要导入 5 个 API Key 吗？" 或 "Are you sure you want to import 5 API Keys?"
```

```vue
<template>
  <h1>{{ t('app.title') }}</h1>
  <button>{{ t('common.save') }}</button>
</template>

<script setup>
import { t } from './locales/index.js'
</script>
```

## 🎨 UI 设计

### 导航栏语言选择器
- 位置：右上角，主题切换按钮和设置按钮之间
- 样式：圆角边框，与应用主题一致
- 图标：国旗表情符号（🇨🇳 / 🇺🇸）
- 交互：悬停时高亮，聚焦时有光晕效果

### 设置界面语言选项
- 位置：外观设置部分的第一个选项
- 标签：🌐 语言
- 描述：切换显示语言
- 控件：下拉选择器
- 样式：与其他设置项保持一致

## 🔄 切换流程

1. 用户选择新语言
2. 调用 `setLocale()` 保存选择
3. 刷新页面以应用新语言
4. 所有组件重新渲染，显示对应语言的文本

## 📝 扩展指南

### 添加新语言

1. 在 `src/locales/` 创建新的语言文件（如 `ja-JP.js`）
2. 复制现有语言包的结构并翻译
3. 在 `index.js` 中导入并注册：
   ```javascript
   import jaJP from './ja-JP.js'
   
   const messages = {
     'zh-CN': zhCN,
     'en-US': enUS,
     'ja-JP': jaJP  // 新增
   }
   ```
4. 在 UI 中添加语言选项

### 添加新翻译键

1. 在所有语言包中添加对应的键值对
2. 确保键名保持一致
3. 使用 `t()` 函数引用新键

## ✅ 最佳实践

1. **始终使用翻译函数** - 不要在代码中硬编码文本
2. **保持键名一致** - 使用点分命名法（如 `settings.title`）
3. **模块化组织** - 按功能模块分组翻译键
4. **参数化文本** - 使用 `{param}` 语法处理动态内容
5. **测试所有语言** - 确保每种语言都正确显示

## 🐛 常见问题

### Q: 切换语言后页面没有立即更新？
A: 这是正常行为，需要刷新页面才能完全应用新语言。

### Q: 如何在不刷新页面的情况下切换语言？
A: 可以使用 Vue 的响应式系统，但需要重构所有组件以支持动态语言切换。当前实现采用刷新页面的简单方案。

### Q: 某些文本没有翻译？
A: 检查翻译键是否正确，确保在所有语言包中都定义了对应的键。

## 🚀 未来改进

- [ ] 支持更多语言（日语、韩语、法语等）
- [ ] 实现无刷新语言切换
- [ ] 添加语言自动检测（基于浏览器设置）
- [ ] 提供翻译贡献指南
- [ ] 添加翻译完整性检查工具

---

*最后更新: 2026-05-12*
